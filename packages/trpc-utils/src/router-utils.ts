/**
 * Router utilities for type-level extraction of router names from AppRouter.
 * These are compile-time only types with no runtime overhead.
 */

export type {
  RouterNames,
  RequiredRouterMapping,
  PartialRouterMapping,
} from './types';
